"use client";

/**
 * lib/cart/useCart.ts
 *
 * Convenience hook — import this anywhere you need cart state or actions.
 * Must be used inside a component wrapped by CartProvider.
 *
 * Usage:
 *   const { cart, addItem, openCart, totalQuantity } = useCart();
 */

export { useCart } from "./CartContext";
